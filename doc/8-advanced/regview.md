# register view

during playback, "Register View" shows the hex data involved with each chip's operation.

![register view dialog](register.png)

hovering over the dialog causes a "hamburger menu" icon to appear at the top-right corner. clicking it pops up a small dialog:
- **Bytes per column**: sets the number of columns displayed. useful for aligning registers for convenient viewing and comparison.